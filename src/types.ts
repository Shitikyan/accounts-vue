export interface Account {
  id: number;
  accountId: number;
  createdAt: string;
  owner: string;
  published: boolean;
  revenue: number;
  spend: number;
  title: string;
  updatedAt: string;
  views: number;
}
